<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE book PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd">
<?asciidoc-toc?>
<?asciidoc-numbered?>
<chapter id="_tutorial">
<title>Tutorial</title>
<simpara>In this chapter we will build on the foundations of the <emphasis>Getting Started</emphasis> guide,
and build a simple web application on top of it. Make sure to have the
<literal>beer-sample</literal> bucket installed, as we&#8217;ll be using it -; the sample application
will allow you to edit and manage various beers and breweries.</simpara>
<note><simpara>The sample application is not entirely complete, and there are some features
which remain to be implemented. Implementing them is an exercise for the
reader.</simpara></note>
<simpara>The full source code is available through couchbaselabs on
<ulink url="https://github.com/couchbaselabs/beersample-python">GitHub</ulink></simpara>
<simpara>Note that the sample application provides more content then described in this
tutorial but it should be simple to navigate while reading this tutorial.</simpara>
<section id="_quickstart">
<title>Quickstart</title>
<itemizedlist>
<listitem>
<simpara>
Ensure you have <emphasis>Flask</emphasis> installed. You can either install it via your
distribution or use <literal>pip install flask</literal>.
</simpara>
</listitem>
<listitem>
<simpara>
<ulink url="http://www.couchbase.com/download">Download Couchbase Server</ulink> and install it.
Make sure to install the <literal>beer-sample</literal> dataset when you run the wizard,
because this tutorial application will work with it.
</simpara>
</listitem>
<listitem>
<simpara>
Clone the repository and <literal>cd</literal> into the directory
</simpara>
<screen>shell&gt; git clone git://github.com/couchbaselabs/beersample-python
Cloning into 'beersample-python'
# ...
shell&gt; cd beersample-python</screen>
</listitem>
<listitem>
<simpara>
Some views need to be set up. You can do this manually via the Web UI, or
invoke the <literal>design_setup.py</literal> script in the sample repository.
</simpara>
<simpara>In the <literal>beer</literal> design document, create a view called <literal>by_name</literal></simpara>
<programlisting language="javascript" linenumbering="unnumbered">function (doc, meta) {
    if (doc.type &amp;&amp; doc.type == "beer") {
        emit(doc.name, null);
    }
}</programlisting>
<simpara>Create a design document called <literal>brewery</literal>. Add a view called <literal>by_name</literal></simpara>
<programlisting language="javascript" linenumbering="unnumbered">function (doc, meta) {
    if (doc.type == "brewery") {
        emit(doc.name, null);
    }
}</programlisting>
</listitem>
<listitem>
<simpara>
Finally, invoke the <literal>beer.py</literal> script
</simpara>
<screen>shell&gt; python beer.py
 * Running on http://0.0.0.0:5000/
 * Restarting with reloader</screen>
</listitem>
<listitem>
<simpara>
Navigate to <literal>localhost:5000</literal> and enjoy the application!
</simpara>
</listitem>
</itemizedlist>
</section>
<section id="_preparations">
<title>Preparations</title>
<simpara>In this section we&#8217;ll talk a bit about setting up your directory layout
and adding some views in the server before we start dealing with the Python
SDK and Flask itself.</simpara>
<section id="_project_setup">
<title>Project Setup</title>
<simpara>Create your project directory, we will call it <literal>beer</literal></simpara>
<screen>shell&gt; mkdir beer
shell&gt; cd beer
shell&gt; mkdir templates
shell&gt; mkdir templates/beer
shell&gt; mkdir templates/brewery
shell&gt; mkdir static
shell&gt; mkdir static/js
shell&gt; mkdir static/css</screen>
<simpara>Showing your directory contents should display something like this</simpara>
<screen>shell&gt; find . -type d
./static
./static/js
./static/css
./static/html
./templates
./templates/brewery
./templates/beer</screen>
<simpara>In order to make the application look pretty, we&#8217;re incorporating jQuery and
Twitter Bootstrap. You can either download the libraries and put it in
their appropriate <literal>css</literal> and <literal>js</literal> directories (under <literal>static</literal>), or clone
the project repository and use it from there. Either way, make sure to have
the following files in place:</simpara>
<itemizedlist>
<listitem>
<simpara>
<literal>static/css/beersample.css</literal>
</simpara>
</listitem>
<listitem>
<simpara>
<literal>static/css/bootstrap.min.css</literal> (the minified twitter bootstrap library)
</simpara>
</listitem>
<listitem>
<simpara>
<literal>static/css/bootstrap-responsive.min.css</literal> (the minified responsive
layout classes from bootstrap)
</simpara>
</listitem>
<listitem>
<simpara>
<literal>static/js/beersample.js</literal>
</simpara>
</listitem>
<listitem>
<simpara>
<literal>static/js/jquery.min.js</literal> (the jQuery javascript library)
</simpara>
</listitem>
</itemizedlist>
<simpara>From here on, you should have a barebones web application configured that
has all the dependencies included. We&#8217;ll now move on and configure the
<literal>beer-sample</literal> bucket the way we need it.</simpara>
</section>
<section id="_preparing_the_views">
<title>Preparing the Views</title>
<simpara>The beer-sample bucket comes with a small set of views already predefined,
but to make our application function correctly we need some more.
This is also a very good chance to explore the view management possibilities
inside the Web-UI.</simpara>
<simpara>Since we want to list beers and breweries by their name, we need to define
one view for each. Head over to the Web-UI and click on the <emphasis>Views</emphasis> menu.
Select <literal>beer-sample</literal> from the dropdown list to switch to the correct bucket.
Now click on <emphasis>Development Views</emphasis> and then <emphasis>Create Development View</emphasis> to
define your first view. You need to give it the name of both the design
document and the actual view. Insert the following names:</simpara>
<itemizedlist>
<listitem>
<simpara>
Design Document Name: <literal>_design/dev_beer</literal>
</simpara>
</listitem>
<listitem>
<simpara>
View Name: <literal>by_name</literal>
</simpara>
</listitem>
</itemizedlist>
<simpara>The next step is to define the <literal>map</literal> and (optional) <literal>reduce</literal> functions. In our
examples, we won&#8217;t use the reduce functions at all but you can play around and
see what happens. Insert the following map function (that&#8217;s JavaScript) and
click <literal>Save</literal>.</simpara>
<programlisting language="javascript" linenumbering="unnumbered">function (doc, meta) {
  if(doc.type &amp;&amp; doc.type == "beer") {
    emit(doc.name, null);
  }
}</programlisting>
<simpara>Every map function takes the full document (<literal>doc</literal>) and its associated metadata
(<literal>meta</literal>) as the arguments. You are then free to inspect this data and emit a
result when you want to have it in your index. In our case we emit the name
of the beer (<literal>doc.name</literal>) when the document both has a <literal>type</literal> field and the
<literal>type</literal> is <literal>beer</literal>. We don&#8217;t need to emit a value - that&#8217;s we we are using
<literal>null</literal> here. It&#8217;s always advisable to keep the index as small as possible.
Resist the urge to include the full document through <literal>emit(meta.id, doc)</literal>,
because it will increase the size of your view indexes. If you need to access
the full document (or large parts), then use the <literal>include_docs</literal> in the <literal>query</literal>
method, which will return <literal>ViewRow</literal> objects together with their documents. You
can also call <literal>cb.get(row.docid)</literal> as well, to get the individual doc for a
single row. The resulting retrieval of the document may be slightly out of
sync with your view, but it will be fast and efficient.</simpara>
<simpara>Now we need to do (nearly) the same for our breweries. Since you already know
how to do this, here is all the information you need to create it:</simpara>
<itemizedlist>
<listitem>
<simpara>
Design Document Name: <literal>_design/dev_brewery</literal>
</simpara>
</listitem>
<listitem>
<simpara>
View Name: <literal>by_name</literal>
</simpara>
</listitem>
<listitem>
<simpara>
Map Function:
</simpara>
<programlisting language="javascript" linenumbering="unnumbered">function (doc, meta) {
  if(doc.type &amp;&amp; doc.type == "brewery") {
    emit(doc.name, null);
  }
}</programlisting>
</listitem>
</itemizedlist>
<simpara>The final step that you need to do is to push the design documents in
production. While the design documents are in development, the index
is only applied on the local node. Since we want to have the index on the whole
dataset, click the <emphasis>Publish</emphasis> button on both design documents (and accept any
info popup that warns you from overriding the old one).</simpara>
<simpara>For more information about using views for indexing and querying from
Couchbase Server, here are some useful resources:</simpara>
<itemizedlist>
<listitem>
<simpara>
General Information: Couchbase Server Manual: Views and Indexes.
</simpara>
</listitem>
<listitem>
<simpara>
Sample Patterns: to see examples and patterns you can use for views, see Couchbase Views, Sample Patterns.
</simpara>
</listitem>
<listitem>
<simpara>
Timestamp Pattern: many developers frequently ask about extracting information based on date or time. To find out more, see Couchbase Views, Sample Patterns.
</simpara>
</listitem>
</itemizedlist>
</section>
<section id="_structure_of_the_flask_application">
<title>Structure of the Flask Application</title>
<simpara>We&#8217;ll be showing bits and pieces of the web app as it pertains to specific sections.
The entire file is less than 300 lines long, and can be inspected by looking
into the <literal>beer.py</literal> file in the repository.</simpara>
<simpara>First, our imports</simpara>
<simpara>We need some extra imports to be able to handle exceptions properly
and let us build better view queries.</simpara>
<formalpara><title>beer.py (imports)</title><para>
<programlisting language="python" linenumbering="unnumbered">from collections import namedtuple
import json

from flask import Flask, request, redirect, abort, render_template

from couchbase import Couchbase
from couchbase.exceptions import KeyExistsError
from couchbase.views.iterator import RowProcessor
from couchbase.views.params import UNSPEC, Query</programlisting>
</para></formalpara>
<simpara>Then, we want to set some constants for our application.</simpara>
<formalpara><title>beer.py (configuration)</title><para>
<screen>DATABASE = 'beer-sample'
HOST = 'localhost'
ENTRIES_PER_PAGE = 30</screen>
</para></formalpara>
<simpara>The <literal>ENTRIES_PER_PAGE</literal> variable is used later on to configure how many beers and
breweries to show in the search results.</simpara>
<simpara>Now, we&#8217;re ready to create our <literal>Flask</literal> application instance</simpara>
<formalpara><title>beer.py (creating the application)</title><para>
<programlisting language="python" linenumbering="unnumbered">app = Flask(__name__, static_url_path='')
app.config.from_object(__name__)</programlisting>
</para></formalpara>
<simpara>The first line creates a new Flask application. The first argument is the
module in which the application is defined. Since we&#8217;re only using a
single file as our application, we can use <literal>__name__</literal> which expands to
the name of the current file being executed (minus the <literal>.py</literal> suffix).</simpara>
<simpara>The second argument instructs Flask to treat unrouted URLs as being
requests for files located in the <literal>static</literal> directory we created
earlier. This will allow our templates to load the required <literal>.js</literal>
and <literal>.css</literal> files.</simpara>
<simpara>The second line creates a configuration object for our <literal>app</literal>. The
argument is the name of the module to scan for configuration directives.
Flask scans this module for variable names in <literal>UPPER_CASE</literal> and places
them in the <literal>app.config</literal> dictionary.</simpara>
<simpara>Then, define a function to give us a database connection</simpara>
<formalpara><title>beer.py (generating a Connection object)</title><para>
<programlisting language="python" linenumbering="unnumbered">def connect_db():
    return Couchbase.connect(
        bucket=app.config['DATABASE'],
        host=app.config['HOST'])

db = connect_db()</programlisting>
</para></formalpara>
<simpara>You already know how to connect to a Couchbase cluster, we&#8217;ll skip the
explanation here.</simpara>
<simpara>The second line sets the module-level <literal>db</literal> variable to be the <literal>Connection</literal> object.
While in larger applications this is probably not a good idea, since this is a
simple app, we can get away with it.</simpara>
</section>
</section>
<section id="_the_welcome_page">
<title>The Welcome Page</title>
<simpara>The first route we will implement is that of the <literal>welcome</literal> page, i.e. the page
which is displayed when someone will go to the root of your site. Since there
is no Couchbase interaction involved, we just tell Flask to render the template.</simpara>
<formalpara><title>beer.py (welcome page)</title><para>
<programlisting language="python" linenumbering="unnumbered">@app.route('/')
def welcome():
    return render_template('welcome.html')

app.add_url_rule('/welcome', view_func=welcome)</programlisting>
</para></formalpara>
<simpara>The <literal>welcome.html</literal> is actually a <emphasis>Jinja</emphasis> template inside the
<literal>templates</literal> directory. Its contents is displayed here:</simpara>
<formalpara><title>templates/welcome.html</title><para>
<screen>{% extends "layout.html" %}
{% block body %}
&lt;div class="span6"&gt;
  &lt;div class="span12"&gt;
    &lt;h4&gt;Browse all Beers&lt;/h4&gt;
    &lt;a href="/beers" class="btn btn-warning"&gt;Show me all beers&lt;/a&gt;
    &lt;hr /&gt;
  &lt;/div&gt;
  &lt;div class="span12"&gt;
    &lt;h4&gt;Browse all Breweries&lt;/h4&gt;
    &lt;a href="/breweries" class="btn btn-info"&gt;Take me to the breweries&lt;/a&gt;
  &lt;/div&gt;
&lt;/div&gt;
&lt;div class="span6"&gt;
&lt;div class="span6"&gt;
  &lt;div class="span12"&gt;
    &lt;h4&gt;About this App&lt;/h4&gt;
    &lt;p&gt;Welcome to Couchbase!&lt;/p&gt;
    &lt;p&gt;This application helps you to get started on application
        development with Couchbase. It shows how to create, update and
        delete documents and how to work with JSON documents.&lt;/p&gt;
  &lt;/div&gt;
&lt;/div&gt;

{% endblock %}</screen>
</para></formalpara>
<simpara>The template simply provides some links to the brewery and beer pages (which are
shown later).</simpara>
<simpara>An interesting thing about this template is that it "inherits" from the common
<literal>layout.html</literal> template. All pages in the beer app will have a common header and
footer to them&#8201;&#8212;&#8201;with only their <literal>body</literal> differing. Here we will show the
<literal>layout.html</literal> template.</simpara>
<formalpara><title>templates/layout.html</title><para>
<screen>&lt;!DOCTYPE HTML&gt;

&lt;html lang="en"&gt;
  &lt;head&gt;
    &lt;meta charset="utf-8"&gt;
    &lt;title&gt;Couchbase Python Beer Sample&lt;/title&gt;
    &lt;meta name="viewport" content="width=device-width, initial-scale=1.0"&gt;
    &lt;meta name="description" content="The Couchbase Java Beer-Sample App"&gt;
    &lt;meta name="author" content="Couchbase, Inc. 2013"&gt;

    &lt;link href="/css/bootstrap.min.css" rel="stylesheet"&gt;
    &lt;link href="/css/beersample.css" rel="stylesheet"&gt;
    &lt;link href="/css/bootstrap-responsive.min.css" rel="stylesheet"&gt;

    &lt;!-- HTML5 shim, for IE6-8 support of HTML5 elements --&gt;
    &lt;!--[if lt IE 9]&gt;
      &lt;script src="http://html5shim.googlecode.com/svn/trunk/html5.js"&gt;&lt;/script&gt;
    &lt;![endif]--&gt;
  &lt;/head&gt;

  &lt;body&gt;
    &lt;div class="container-narrow"&gt;
      &lt;div class="masthead"&gt;
        &lt;ul class="nav nav-pills pull-right"&gt;
          &lt;li&gt;&lt;a href="/welcome"&gt;Home&lt;/a&gt;&lt;/li&gt;
          &lt;li&gt;&lt;a href="/beers"&gt;Beers&lt;/a&gt;&lt;/li&gt;
          &lt;li&gt;&lt;a href="/breweries"&gt;Breweries&lt;/a&gt;&lt;/li&gt;
        &lt;/ul&gt;
        &lt;h2 class="muted"&gt;Couchbase Beer Sample&lt;/h2&gt;
      &lt;/div&gt;
      &lt;hr&gt;
      &lt;div class="row-fluid"&gt;
        &lt;div class="span12"&gt;
            {% block body %}{% endblock %}
        &lt;/div&gt;
      &lt;/div&gt;
      &lt;hr&gt;
      &lt;div class="footer"&gt;
        &lt;p&gt;&amp;copy; Couchbase, Inc. 2013&lt;/p&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    &lt;script src="/js/jquery.min.js"&gt;&lt;/script&gt;
    &lt;script src="/js/bootstrap.min.js"&gt;&lt;/script&gt;
    &lt;script src="/js/beersample.js"&gt;&lt;/script&gt;
  &lt;/body&gt;
&lt;/html&gt;</screen>
</para></formalpara>
<simpara>If you start your app now, you should be able to navigate to <literal>localhost:5000</literal>
and see the welcome page. You&#8217;ll get a 404 if you try to visit any links though -
this is because we haven&#8217;t implemented them yet. Let&#8217;s do that now!</simpara>
</section>
<section id="_managing_beers">
<title>Managing Beers</title>
<simpara>In this section we&#8217;ll show the construction of the webapp in respect to managing
beers. We&#8217;ll be able to list, inspect, edit, create, search, and delete beers.</simpara>
<section id="_showing_beers">
<title>Showing Beers</title>
<simpara>Now we&#8217;re finally getting into the cooler stuff of this tutorial.</simpara>
<simpara>First, we&#8217;ll implement several classes for our pages to use.</simpara>
<formalpara><title>beer.py (custom Beer row class and processing)</title><para>
<programlisting language="python" linenumbering="unnumbered">class Beer(object):
    def __init__(self, id, name, doc=None):
        self.id = id
        self.name = name
        self.brewery = None
        self.doc = doc

    def __getattr__(self, name):
        if not self.doc:
            return ""
        return self.doc.get(name, "")


class BeerListRowProcessor(object):
    """
    This is the row processor for listing all beers (with their brewery IDs).
    """
    def handle_rows(self, rows, connection, include_docs):
        ret = []
        by_docids = {}

        for r in rows:
            b = Beer(r['id'], r['key'])
            ret.append(b)
            by_docids[b.id] = b

        keys_to_fetch = [ x.id for x in ret ]
        docs = connection.get_multi(keys_to_fetch, quiet=True)

        for beer_id, doc in docs.items():
            if not doc.success:
                ret.remove(beer)
                continue

            beer = by_docids[beer_id]
            beer.brewery_id = doc.value['brewery_id']

        return ret</programlisting>
</para></formalpara>
<simpara>First, we declare a simple <literal>Beer</literal> object. This isn&#8217;t too fancy and we
could&#8217;ve probably just used a simple <literal>dict</literal> - however it allows us
to demonstrate the use of the <literal>RowProcessor</literal> interface (defined next).</simpara>
<simpara>In the beer listing page, we want to display each beer along with a link
to the brewery that produces it. However, we&#8217;ve defined the <literal>beer/by_name</literal>
view to only return the name of the beer. In order to obtain the brewery
we need to fetch each beer document and examine it. The document will
contain the Brewery ID which we can then use later on.</simpara>
<simpara>The <literal>BeerListRowProcessor</literal> is an implementation of the <literal>RowProcessor</literal>
interface which operates on the returned view rows.</simpara>
<simpara>For each raw JSON row, it creates a new <literal>Beer</literal> object; the first argument
is the document ID - which is used to provide a link to display more information
about the beer. The second is the name of the beer itself which we use in the
beer list on the webpage.</simpara>
<simpara>We also create a local variable called <literal>by_docids</literal> - this will allow us to get
a <literal>Beer</literal> object by its document ID- for reasons we will soon see.</simpara>
<simpara>After we&#8217;ve created all the beers, we create a list of document IDs to fetch
by using list comprehension. We pass this list to <literal>get_multi</literal> (passing
<literal>quiet=True</literal>, as there may be some inconsistencies between view indexes and
the actual documents).</simpara>
<simpara>While we could have made this simpler by performing an individual <literal>get</literal> on
each <literal>beer.id</literal>, this would have been less efficient in terms of network usage.</simpara>
<simpara>Now that we have the beer documents, it&#8217;s time to set each beer&#8217;s <literal>brewery_id</literal>
to its relevant value.</simpara>
<simpara>We first check to see that each document was successful in being retrieved; then
we look up the corresponding <literal>Beer</literal> object by getting it from the <literal>by_docids</literal>
dictionary using the <literal>beer_id</literal> as the key.</simpara>
<simpara>Then, we extract the <literal>brewery_id</literal> field from the document and place it into the
<literal>Beer</literal> object.</simpara>
<simpara>Finally, we return the list of populated beers. The <literal>View</literal> object (returned by
the <literal>query</literal> function) will now yield results from it as we iterate over it.</simpara>
<simpara>Before we forget, let&#8217;s put this all together:</simpara>
<formalpara><title>beer.py (showing beer listings)</title><para>
<programlisting language="python" linenumbering="unnumbered">@app.route('/beers')
def beers():
    rp = BeerListRowProcessor()
    rows = db.query("beer", "by_name",
                    limit=ENTRIES_PER_PAGE,
                    row_processor=rp)

    return render_template('beer/index.html', results=rows)</programlisting>
</para></formalpara>
<simpara>We tell flask to route requests to <literal>/beers</literal> to this function.
We create an instance of the <literal>BeerListRowProcessor</literal> function we just defined
above.</simpara>
<simpara>We then execute a view query using the <literal>query</literal> method; passing it the name of the
design and view (<literal>beer</literal> and <literal>by_name</literal>, respsectively).</simpara>
<simpara>We set the <literal>limit</literal> directive to the aforementioned <literal>ENTRIES_PER_PAGE</literal> directive;
so as not to flood a single webpage with many results.</simpara>
<simpara>We finally tell the <literal>query</literal> method to use our own <literal>BeerListRowProcessor</literal> for
processing the results.</simpara>
<simpara>We then direct the template engine to render the <literal>beer/index.html</literal> template,
setting the template variable <literal>rows</literal> to the iterable returned by the <literal>query</literal>
function.</simpara>
<simpara>Here is the <literal>beer/index.html</literal> template:</simpara>
<formalpara><title>templates/beer/index.html</title><para>
<screen>{% extends "layout.html" %}
{% block body %}

&lt;h3&gt;Browse Beers&lt;/h3&gt;
&lt;form class="navbar-search pull-left"&gt;
    &lt;input id="beer-search" type="text" class="search-query" placeholder="Search for Beers"&gt;
&lt;/form&gt;


&lt;table id="beer-table" class="table table-striped"&gt;
    &lt;thead&gt;
        &lt;tr&gt;
            &lt;th&gt;Name&lt;/th&gt;
            &lt;th&gt;Brewery&lt;/th&gt;
            &lt;th&gt;&lt;/th&gt;
        &lt;/tr&gt;
    &lt;/thead&gt;
    &lt;tbody&gt;
        {% for beer in results %}
        &lt;tr&gt;
            &lt;td&gt;&lt;a href="/beers/show/{{beer.id}}"&gt;{{beer.name}}&lt;/a&gt;&lt;/td&gt;
            &lt;td&gt;&lt;a href="/breweries/show/{{beer.brewery_id}}"&gt;To Brewery&lt;/a&gt;&lt;/td&gt;
            &lt;td&gt;
                &lt;a class="btn btn-small btn-warning" href="/beers/edit/{{beer.id}}"&gt;Edit&lt;/a&gt;
                &lt;a class="btn btn-small btn-danger" href="/beers/delete/{{beer.id}}"&gt;Delete&lt;/a&gt;
            &lt;/td&gt;
        &lt;/tr&gt;
        {% endfor %}
    &lt;/tbody&gt;
&lt;/table&gt;

&lt;div&gt;
    &lt;a class="btn btn-small btn-success" href="/beers/create"&gt;Add Beer&lt;/a&gt;
&lt;/div&gt;

{% endblock %}</screen>
</para></formalpara>
<simpara>We&#8217;re using <emphasis>Jinja</emphasis> <literal>{% for %}</literal> blocks to iterate and emit a fragment of HTML
for each <literal>Beer</literal> object returned by the query.</simpara>
<simpara>If you navigate to <literal>localhost:5000/beers</literal>, you&#8217;ll see a listing of beers now.
Each beer will have an <literal>To Brewery</literal>, <literal>Edit</literal>, and <literal>Delete</literal> button.</simpara>
<simpara>On the bottom of the page, you can also see a button <literal>Add Beer</literal> which will allow
you to define new beers.</simpara>
<simpara>Let&#8217;s implement the <literal>Delete</literal> button next!</simpara>
</section>
<section id="_deleting_beers">
<title>Deleting Beers</title>
<simpara>Due to the simplicity of Couchbase and Flask, we can implement a single method
to delete both beers and breweries.</simpara>
<formalpara><title>beer.py (deleting a beer)</title><para>
<screen>@app.route('&lt;otype&gt;/delete/&lt;id&gt;')
def delete_object(otype, id):
    try:
        db.delete(id)
        return redirect('/welcome')

    except NotFoundError:
        return "No such {0} '{1}'".format(otype, id), 404</screen>
</para></formalpara>
<simpara>Here we tell flask to route any URL which has as its second component the string
<literal>delete</literal> to this method. The paths in <literal>&lt;angle brackets&gt;</literal> are routing tokens
which flask passes to the handler as arguments. This means that URLs such as
<literal>/beers/delete/foobar</literal>, <literal>/foo/delete/whatever</literal> etc. are all routed to here.</simpara>
<simpara>When we get an ID, we try to delete it by using the <literal>delete</literal> method. We use a
<literal>try</literal> block. If successful, we redirect to the welcome page; but if the key
does not exist, we return with an error message and a <literal>404</literal> status code.</simpara>
<simpara>You can now access this page by going to <literal>localhost:5000/beers/delete/nonexistent</literal>
and get a 404. Or you can delete a beer by clicking on one of the <literal>Delete</literal> buttons
in the <literal>/beers</literal> page!</simpara>
</section>
<section id="_displaying_beers">
<title>Displaying Beers</title>
<simpara>Here we will demonstrate how you can display the beers. In this case, we display
a page showing all the fields and values of a given beer.</simpara>
<formalpara><title>beer.py (showing a single beer)</title><para>
<programlisting language="python" linenumbering="unnumbered">@app.route('/beers/show/&lt;beer_id&gt;')
def show_beer(beer_id):
    doc = db.get(beer_id, quiet=True)
    if not doc.success:
        return "No such beer {0}".format(beer_id), 404


    return render_template(
        'beer/show.html',
        beer=Beer(beer_id, doc.value['name'], doc.value))</programlisting>
</para></formalpara>
<simpara>Like for the <literal>delete</literal> action, we first check to see that the beer exists.
We are passed the beer ID as the last part of the URL - this is passed to
us as the <literal>beer_id</literal>.</simpara>
<simpara>In order to display the information for the given beer ID, we simply call
the connection&#8217;s <literal>get</literal> method with the <literal>beer_id</literal> argument. We also pass
the <literal>quiet</literal> parameter so that we don&#8217;t receive an exception if the beer
does not exist.</simpara>
<simpara>We then check to see that the <literal>success</literal> property of the returned <literal>Result</literal>
object is true. If it isn&#8217;t we return an HTTP <literal>404</literal> error.</simpara>
<simpara>If the beer exists, we construct a new <literal>Beer</literal> object; passing it the ID
and the <literal>name</literal> field within the value dictionary.</simpara>
<simpara>We then pass this beer to the <literal>templates/beer/show.html</literal> template which
we&#8217;ll show here:</simpara>
<formalpara><title>templates/beer/show.html</title><para>
<screen>{% extends "layout.html" %}
{% block body %}

{% set display = beer.doc %}
{% set brewery_id = display['brewery_id'] %}

&lt;h3&gt;Show Details for Beer "{{beer.name}}"&lt;/h3&gt;
&lt;table class="table table-striped"&gt;
    &lt;tbody&gt;
        &lt;tr&gt;
            &lt;td&gt;&lt;strong&gt;brewery_id&lt;/strong&gt;&lt;/td&gt;
            &lt;td&gt;&lt;a href="/breweries/show/{{brewery_id}}"&gt;{{brewery_id}}&lt;/a&gt;&lt;/td&gt;
        &lt;/tr&gt;
        {% for k, v in display.items() if not k == "brewery_id" %}
        &lt;tr&gt;
            &lt;td&gt;&lt;strong&gt;{{k}}&lt;/strong&gt;&lt;/td&gt;
            &lt;td&gt;{{v}}&lt;/td&gt;
        &lt;/tr&gt;
        {% endfor %}
    &lt;/tbody&gt;
&lt;/table&gt;

&lt;a class="btn btn-medium btn-warning"
    href="/beers/edit/{{beer.id}}"&gt;Edit&lt;/a&gt;
&lt;a class="btn btn-medium btn-danger"
    href="/beers/delete/{{beer.id}}"&gt;Delete&lt;/a&gt;

{% endblock %}</screen>
</para></formalpara>
<simpara>Here we make the <literal>display</literal> variable in a special <literal>{% set %}</literal> directive.
This makes dealing with the rest of the code simpler.</simpara>
<simpara>The next thing we do is extract the <literal>brewery_id</literal>, and create a special
entry with a link pointing to the page to display the actual brewery.</simpara>
<simpara>The next thing we do is iterate over the rest of the fields (omitting the
brewery ID); printing out the key and value of each.</simpara>
<simpara>Finally, we provide links at the bottom to <literal>Edit</literal> and <literal>Delete</literal> the beer.</simpara>
</section>
<section id="_editing_beers">
<title>Editing Beers</title>
<formalpara><title>beer.py (beer edit page)</title><para>
<programlisting language="python" linenumbering="unnumbered">def normalize_beer_fields(form):
    doc = {}
    for k, v in form.items():
        name_base, fieldname = k.split('_', 1)
        if name_base != 'beer':
            continue

        doc[fieldname] = v

    if not 'name' in doc or not doc['name']:
        return (None, ("Must have name", 400))

    if not 'brewery_id' in doc or not doc['brewery_id']:
        return (None, ("Must have brewery ID", 400))

    if not db.get(doc['brewery_id'], quiet=True).success:
        return (None,
                ("Brewery ID {0} not found".format(doc['brewery_id']), 400))

    return doc, None

@app.route('/beers/edit/&lt;beer&gt;', methods=['GET'])
def edit_beer_display(beer):
    bdoc = db.get(beer, quiet=True)
    if not bdoc.success:
        return "No Such Beer", 404

    return render_template('beer/edit.html',
                           beer=Beer(beer, bdoc.value['name'], bdoc.value),
                           posturl='/beers/edit/' + beer,
                           is_create=False)


@app.route('/beers/edit/&lt;beer&gt;', methods=['POST'])
def edit_beer_submit(beer):
    doc, err = normalize_beer_fields(request.form)

    if not doc:
        return err

    db.set(beer, doc)
    return redirect('/beers/show/' + beer)</programlisting>
</para></formalpara>
<simpara>We define two handlers for editing. The first is the <literal>GET</literal> method for
<literal>/beers/edit/&lt;beer&gt;</literal> which displays a nice HTML form in which we can
use to edit it. It passes the template the <literal>Beer</literal> object, a boolean
parameter indicating that this is <emphasis>not</emphasis> a new beer (as the same template is
also used for the <literal>Create Beer</literal> form), and finally the URL to <literal>POST</literal> to when
the form is submitted.</simpara>
<simpara>The second is the <literal>POST</literal> handler which validates the input. The post handler
calls the <literal>normalize_beer_fields</literal> function.</simpara>
<simpara>This function converts
the form fields into properly formed names for the beer document; then it checks
to see that the beer has a valid <literal>name</literal>. It then checks to see that a
<literal>brewery_id</literal> was specified and that it indeed exists.
Once these checks have passed, it returns a tuple of (<literal>doc</literal>, <literal>None</literal>).</simpara>
<simpara>The <literal>POST</literal> handler checks to see that the second element of the tuple
is false - if it isn&#8217;t, then it&#8217;s an error code, and the first element
becomes the error message.</simpara>
<simpara>Otherwise, the first element becomes the document.</simpara>
<simpara>It then sets the document in Couchbase using the <literal>set</literal> method.</simpara>
<simpara>The template is rather wordy as we enumerate all the possible fields with a nice
description :)</simpara>
<formalpara><title>templates/beer/edit.html</title><para>
<screen>{% extends "layout.html" %}
{% block body %}

{% if is_create %}
&lt;h3&gt;Create Beer&lt;/h3&gt;
{% else %}
&lt;h3&gt;Editing {{beer.name}}&lt;/h3&gt;
{% endif %}

&lt;form method="post" action=""&gt;
  &lt;fieldset&gt;
    &lt;legend&gt;General Info&lt;/legend&gt;
    &lt;div class="span12"&gt;
      &lt;div class="span6"&gt;
        &lt;label&gt;Name&lt;/label&gt;
        &lt;input type="text" name="beer_name" placeholder="The name of the beer." value="{{beer.name}}"&gt;

        &lt;label&gt;Description&lt;/label&gt;
        &lt;input type="text" name="beer_description" placeholder="A short description." value="{{beer.description}}"&gt;
      &lt;/div&gt;
      &lt;div class="span6"&gt;
        &lt;label&gt;Style&lt;/label&gt;
        &lt;input type="text" name="beer_style" placeholder="Bitter? Sweet? Hoppy?" value="{{beer.style}}"&gt;

        &lt;label&gt;Category&lt;/label&gt;
        &lt;input type="text" name="beer_category" placeholder="Ale? Stout? Lager?" value="{{beer.category}}"&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/fieldset&gt;
  &lt;fieldset&gt;
    &lt;legend&gt;Details&lt;/legend&gt;
    &lt;div class="span12"&gt;
      &lt;div class="span6"&gt;
        &lt;label&gt;Alcohol (ABV)&lt;/label&gt;
        &lt;input type="text" name="beer_abv" placeholder="The beer's ABV" value="{{beer.abv}}"&gt;

        &lt;label&gt;Biterness (IBU)&lt;/label&gt;
        &lt;input type="text" name="beer_ibu" placeholder="The beer's IBU" value="{{beer.ibu}}"&gt;
      &lt;/div&gt;
      &lt;div class="span6"&gt;
        &lt;label&gt;Beer Color (SRM)&lt;/label&gt;
        &lt;input type="text" name="beer_srm" placeholder="The beer's SRM" value="{{beer.srm}}"&gt;

        &lt;label&gt;Universal Product Code (UPC)&lt;/label&gt;
        &lt;input type="text" name="beer_upc" placeholder="The beer's UPC" value="{{beer.upc}}"&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/fieldset&gt;
  &lt;fieldset&gt;
    &lt;legend&gt;Brewery&lt;/legend&gt;
    &lt;div class="span12"&gt;
      &lt;div class="span6"&gt;
        &lt;label&gt;Brewery&lt;/label&gt;
        &lt;input type="text" name="beer_brewery_id" placeholder="The brewery" value="{{beer.brewery_id}}"&gt;
      &lt;/div&gt;
    &lt;/div&gt;
  &lt;/fieldset&gt;
  &lt;div class="form-actions"&gt;
      &lt;button type="submit" class="btn btn-primary"&gt;Save changes&lt;/button&gt;
  &lt;/div&gt;
&lt;/form&gt;

{% endblock %}</screen>
</para></formalpara>
<simpara>The template first checks the <literal>is_create</literal> variable - if it&#8217;s <literal>False</literal>, then we&#8217;re
editing an existing beer, and the caption is filled with that name. Otherwise,
it&#8217;s titled as <literal>Create Beer</literal>.</simpara>
</section>
<section id="_creating_beers">
<title>Creating Beers</title>
<simpara>This is largely the same as editing beers:</simpara>
<formalpara><title>beer.py (create beer page)</title><para>
<screen>@app.route('/beers/create')
def create_beer_display():
    return render_template('beer/edit.html', beer=Beer('', ''), is_create=True)


@app.route('/beers/create', methods=['POST'])
def create_beer_submit():
    doc, err = normalize_beer_fields(request.form)

    if not doc:
        return err

    id = '{0}-{1}'.format(doc['brewery_id'],
                          doc['name'].replace(' ', '_').lower())

    try:
        db.add(id, doc)
        return redirect('/beers/show/' + id)

    except KeyExistsError:
        return "Beer already exists!", 400</screen>
</para></formalpara>
<simpara>Here we display the same form as the one for editing beers, except we set the
<literal>is_create</literal> parameter to True, and pass an empty <literal>Beer</literal> object - needed because
the template still tries to populate the form fields with <emphasis>existing</emphasis> values.</simpara>
<simpara>In the <literal>POST</literal> handler, we call <literal>normalize_beer_field</literal> as above when editing
beers.</simpara>
<simpara>Since we&#8217;re creating a <emphasis>new</emphasis> beer, we use the <literal>add</literal> method instead. This will
raise an exception if the beer already exists. We catch this and display it to
the user.</simpara>
<simpara>If all things went well, the user is redirected to the beer display page for the
newly created beer.</simpara>
</section>
<section id="_searching_beers">
<title>Searching Beers</title>
<simpara>In the beer listing page above, you may have noticed a search box at the top.
We can use it to dynamically filter our table based on user input. We&#8217;ll use
<emphasis>Javascript</emphasis> at the client layer to perform the querying and filtering, and
views with range queries at the server (flask) layer to return the results.</simpara>
<simpara>Before we implement the Python-level search method, we need to put the following
in the <literal>static/js/beersample.js</literal> file (if it&#8217;s not there already) to listen
on searchbox changes and update the table with the resulting JSON (which will
be returned from the search method):</simpara>
<formalpara><title>static/js/beersample.js (snippet)</title><para>
<programlisting language="javascript" linenumbering="unnumbered">$(document).ready(function() {

    /**
     * AJAX Beer Search Filter
     */
    $("#beer-search").keyup(function() {
       var content = $("#beer-search").val();
       if(content.length &gt;= 0) {
           $.getJSON("/beers/search", {"value": content}, function(data) {
               $("#beer-table tbody tr").remove();
               for(var i=0;i&lt;data.length;i++) {
                   var html = "&lt;tr&gt;";
                   html += "&lt;td&gt;&lt;a href=\"/beers/show/"+data[i].id+"\"&gt;"+data[i].name+"&lt;/a&gt;&lt;/td&gt;";
                   html += "&lt;td&gt;&lt;a href=\"/breweries/show/"+data[i].brewery+"\"&gt;To Brewery&lt;/a&gt;&lt;/td&gt;";
                   html += "&lt;td&gt;";
                   html += "&lt;a class=\"btn btn-small btn-warning\" href=\"/beers/edit/"+data[i].id+"\"&gt;Edit&lt;/a&gt;\n";
                   html += "&lt;a class=\"btn btn-small btn-danger\" href=\"/beers/delete/"+data[i].id+"\"&gt;Delete&lt;/a&gt;";
                   html += "&lt;/td&gt;";
                   html += "&lt;/tr&gt;";
                   $("#beer-table tbody").append(html);
               }
           });
       }
    });
});</programlisting>
</para></formalpara>
<simpara>The code waits for keyup events on the search field, and if they happen, it
issues an <emphasis>AJAX</emphasis> query on the search function within the app. The search
handler computes the result (using views) and returns it as JSON. The
JavaScript then clears the table, iterates over the results, and creates
new rows.</simpara>
<simpara>The search handler looks like this:</simpara>
<formalpara><title>beer.py (ajax search response)</title><para>
<programlisting language="python" linenumbering="unnumbered">def return_search_json(ret):
    response = app.make_response(json.dumps(ret))
    response.headers['Content-Type'] = 'application/json'
    return response

@app.route('/beers/search')
def beer_search():
    value = request.args.get('value')
    q = Query()
    q.mapkey_range = [value, value + Query.STRING_RANGE_END]
    q.limit = ENTRIES_PER_PAGE

    ret = []

    rp = BeerListRowProcessor()
    res = db.query("beer", "by_name",
                   row_processor=rp,
                   query=q,
                   include_docs=True)

    for beer in res:
        ret.append({'id' : beer.id,
                    'name' : beer.name,
                    'brewery' : beer.brewery_id})

    return return_search_json(ret)</programlisting>
</para></formalpara>
<simpara>The <literal>beer_search</literal> function first extracts the user input by examining the query
string from the request.</simpara>
<simpara>It then creates a <literal>Query</literal> object; the <literal>Query</literal> object then has its <literal>mapkey_range</literal>
property set to a list of two elements; the first is the user input, and the
second is the user input with the magic <literal>STRING_RANGE_END</literal> string appended to it.
This form of range indicates that all keys which start with the user input
(<literal>value</literal>) will be returned. If we just provided a single element, results
would also contain matches which are lexically "greater" than the user input;
if we just provided the same value for the second and first elements, only
items which matched the string exactly would be returned.</simpara>
<simpara>The special <literal>STRING_RANGE_END</literal> is actually a <literal>u"\uEFF"</literal> UTF-8 character, which
for the view engine means "end here". You need to get used to it a bit, but it&#8217;s
actually very neat and efficient.</simpara>
<simpara>We re-use our <literal>BeerListRowProcessor</literal> class to filter the results here (as the
data required is the same as that of the beer listing (<literal>beer/index.html</literal>) page.</simpara>
<simpara>However we need to return a JSON array of</simpara>
<programlisting language="javascript" linenumbering="unnumbered">{ "id" : "beer_id", "name" : "beer_name", "brewery" : "the_brewery_id" }</programlisting>
<simpara>so we need to convert the rows into JSON first. This is done by the
<literal>return_search_json</literal> function.</simpara>
<simpara>Now your search box should work nicely.</simpara>
</section>
</section>
<section id="_managing_breweries">
<title>Managing Breweries</title>
<simpara>While this is implemented in the repository above, it is left as an exercise to
the reader to work out some more details.</simpara>
</section>
<section id="_wrapping_up">
<title>Wrapping Up</title>
<simpara>The tutorial presented an easy approach to start a web application with
Couchbase Server as the underlying data source. If you want to dig a
little bit deeper, the full source code on couchbaselabs on GitHub has
more code to learn from. This may be extended and updated from time to time.</simpara>
<simpara>Of course, this is only the starting point for Couchbase, but together
with the Getting Started Guide, you should now be well equipped to
start exploring Couchbase Server on your own. Have fun working with Couchbase!</simpara>
<section id="_food_for_thought">
<title>Food For Thought</title>
<simpara>There are some things still not implemented in the example; here is some food
for thought.</simpara>
<itemizedlist>
<listitem>
<simpara>
When deleting a brewery, ensure it has no beers dependent on it.
</simpara>
</listitem>
<listitem>
<simpara>
Provide a search where one can query beers beloning to a given brewery
</simpara>
</listitem>
<listitem>
<simpara>
Handle concurrent updates to a beer and/or brewery
</simpara>
</listitem>
<listitem>
<simpara>
Implement a <emphasis>like</emphasis> feature, where one can like a beer or a brewery;
likewise, they can unlike one as well!
</simpara>
</listitem>
</itemizedlist>
</section>
</section>
</chapter>
